// The "defence.gov.ng" domain appears in the public site's own canonical/meta
// tags, but that's a placeholder — it isn't where anything is actually
// deployed. This is the real, currently-deployed URL, used both to resolve
// relative image paths in the admin and to build preview links out to the
// live pages.
export const PUBLIC_SITE_URL = 'https://mod3-nine.vercel.app';
